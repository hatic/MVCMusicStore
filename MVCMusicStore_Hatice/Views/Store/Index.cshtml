@model IEnumerable<MVCMusicStore_Hatice.Models.Genre>
 @{
     ViewBag.Title = "Store"; 
     }
<h3>Browse Genres</h3>
<ul>
    @foreach (var genre in Model) { 
    <li><a href="/Store/Browse?genre=@genre.Name">@genre.Name</a></li>
    
    }
    </ul>